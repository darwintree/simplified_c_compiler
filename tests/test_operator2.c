// how to use: copy the following codes as input manually
// 取模位运算
int main()
{
    int a, b, c;
    a = 1;
    b = 2;
    int c = a  (b - 2 * a) / a;
    if (c == 1) {
        write(c)
    }
    return 0;
}